# ================================
# AESA 환경변수 템플릿
# ================================
# 이 파일을 .env로 복사한 후 값을 설정하세요.
# cp .env.example .env

# 데모 접근 키 (API 인증용)
DEMO_API_KEY=your_demo_key_here

# 엄격 모드 (EMOTIONS.json 단일 진실원 사용)
STRICT_SSOT=1

# CUDA 사용 여부 (GPU 가속)
USE_CUDA=1

# 프로파일 모드 (dev/pilot/prod)
EA_PROFILE=prod
